// -----------------------------------------------------------------------------
// Imports

const Utils = require( '../utils' );

module.exports = {
  ColumnAlignment: [ 'l', 'c' ],
  Headings: Utils.getHeadings( 'Module Name', 'Installed?' )
};
